use super::pwm::PositionWeightMatrix;

pub struct GibbsSampling {}

impl GibbsSampling {
    pub fn find(k: usize, seqs: Vec<String>) -> PositionWeightMatrix {
        // Testdata
        PositionWeightMatrix::default()
    }
}
